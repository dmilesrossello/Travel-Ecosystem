<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Travel Technology Ecosystem — Value Chain</title>
<link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;1,400&display=swap" rel="stylesheet">
<style>
:root {
  --teal: #009B9B;
  --teal-light: #E6F5F5;
  --teal-mid: #B3E0E0;
  --dark: #1A1A2E;
  --text: #1A1A2E;
  --text-mid: #555570;
  --text-light: #8888A0;
  --bg: #FFFFFF;
  --bg-off: #F7F8FA;
  --border: #E2E4EA;
  --border-light: #EDEEF2;
  --green: #00A676;
  --green-bg: #E8F7F1;
  --green-border: #B5E4D0;
  --yellow: #D4920B;
  --yellow-bg: #FFF8E8;
  --yellow-border: #F0D68A;
  --red-bg: #F5F0F0;
  --red-border: #D8CCCC;
  --purple: #6B4FBB;
  --purple-bg: #F0ECF9;
  --purple-border: #C5B8E8;
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.06);
  --shadow-md: 0 2px 8px rgba(0,0,0,0.08);
}

* { margin: 0; padding: 0; box-sizing: border-box; }

body {
  background: var(--bg);
  color: var(--text);
  font-family: 'Plus Jakarta Sans', -apple-system, sans-serif;
  line-height: 1.5;
  -webkit-font-smoothing: antialiased;
}

.page-header {
  padding: 36px 48px 0;
  max-width: 1600px;
  margin: 0 auto;
}

.tag-row {
  display: flex;
  align-items: center;
  gap: 10px;
  margin-bottom: 14px;
}

.tag {
  background: var(--dark);
  color: white;
  font-size: 0.6rem;
  font-weight: 700;
  padding: 3px 10px;
  border-radius: 3px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.tag.teal { background: var(--teal); }

.page-header h1 {
  font-size: 1.65rem;
  font-weight: 800;
  color: var(--dark);
  line-height: 1.25;
  max-width: 1100px;
}

.page-header h1 em { font-style: normal; color: var(--teal); }

.page-header .subtitle {
  font-size: 0.82rem;
  color: var(--text-mid);
  margin-top: 5px;
}

.legend-strip {
  display: flex;
  align-items: center;
  gap: 22px;
  padding: 14px 48px;
  max-width: 1600px;
  margin: 0 auto;
  border-bottom: 1px solid var(--border-light);
  flex-wrap: wrap;
}

.legend-title {
  font-size: 0.66rem;
  font-weight: 700;
  color: var(--text-light);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.legend-item {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 0.68rem;
  color: var(--text-mid);
}

.legend-dot {
  width: 9px; height: 9px; border-radius: 3px;
}

.legend-dot.core { background: var(--green); }
.legend-dot.partial { background: var(--yellow); }
.legend-dot.gap { background: var(--border); border: 1px dashed var(--text-light); }
.legend-dot.future { background: var(--purple); }

.legend-sep {
  width: 1px; height: 14px; background: var(--border);
}

.legend-arrow {
  display: flex; align-items: center; gap: 5px; font-size: 0.65rem; color: var(--text-light);
}

.legend-arrow .al { width: 20px; height: 3px; border-radius: 2px; }
.legend-arrow .al.solid { background: var(--teal); }
.legend-arrow .al.dashed { background: repeating-linear-gradient(90deg, var(--text-light) 0 3px, transparent 3px 6px); }

.controls {
  display: flex;
  justify-content: center;
  gap: 5px;
  padding: 16px 48px 10px;
}

.ctrl-btn {
  background: var(--bg-off);
  border: 1px solid var(--border);
  color: var(--text-mid);
  padding: 6px 16px;
  border-radius: 5px;
  font-family: inherit;
  font-size: 0.72rem;
  font-weight: 600;
  cursor: pointer;
  transition: all 0.2s;
}

.ctrl-btn.active { background: var(--teal); border-color: var(--teal); color: white; }
.ctrl-btn:hover:not(.active) { background: var(--teal-light); border-color: var(--teal-mid); color: var(--teal); }

.canvas-wrap {
  max-width: 1600px;
  margin: 0 auto;
  padding: 6px 32px 48px;
  overflow-x: auto;
}

/* ===== VALUE CHAIN VIEW ===== */
#vc { min-width: 1150px; }

.swim-lane {
  display: flex;
  align-items: stretch;
  min-height: 80px;
}

.lane-label {
  width: 130px;
  min-width: 130px;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 10px 6px;
  font-size: 0.68rem;
  font-weight: 700;
  color: white;
  text-align: center;
  line-height: 1.25;
}

.lane-label.s1 { background: var(--teal); }
.lane-label.s2 { background: #0086A8; }
.lane-label.s3 { background: #006B8A; }
.lane-label.s4 { background: #00526D; }
.lane-label.s5 { background: #3A4A5C; }
.lane-label.s6 { background: #2D3044; }
.lane-label.s7 { background: #1A1A2E; }

.lane-body {
  flex: 1;
  display: flex;
  flex-wrap: wrap;
  gap: 7px;
  padding: 10px 14px;
  border-bottom: 1px solid var(--border-light);
  align-items: flex-start;
  align-content: flex-start;
}

.grp {
  border: 1.5px solid var(--border);
  border-radius: 7px;
  padding: 8px 9px;
  background: var(--bg-off);
  display: flex;
  flex-wrap: wrap;
  gap: 6px;
  position: relative;
}

.grp .gl {
  position: absolute;
  top: -8px; left: 10px;
  background: var(--bg-off);
  padding: 0 5px;
  font-size: 0.58rem;
  font-weight: 700;
  color: var(--text-light);
  text-transform: uppercase;
  letter-spacing: 0.03em;
}

.seg {
  background: var(--bg);
  border: 1.5px solid var(--border);
  border-radius: 5px;
  padding: 7px 10px;
  min-width: 100px;
  max-width: 240px;
  transition: all 0.2s;
  cursor: default;
}

.seg:hover {
  border-color: var(--teal-mid);
  box-shadow: var(--shadow-md);
  transform: translateY(-1px);
}

.seg.jh {
  border-color: var(--teal);
  background: var(--teal-light);
  position: relative;
}

.seg.jh::after {
  content: '';
  position: absolute;
  top: -1.5px; left: -1.5px; right: -1.5px;
  height: 3px;
  background: var(--teal);
  border-radius: 5px 5px 0 0;
}

.seg .sh {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 5px;
}

.seg .sn {
  font-size: 0.7rem;
  font-weight: 700;
  color: var(--dark);
  line-height: 1.2;
}

.sb {
  font-size: 0.55rem;
  font-weight: 700;
  padding: 1.5px 6px;
  border-radius: 3px;
  white-space: nowrap;
  flex-shrink: 0;
}

.sb.core { background: var(--green-bg); color: var(--green); border: 1px solid var(--green-border); }
.sb.partial { background: var(--yellow-bg); color: var(--yellow); border: 1px solid var(--yellow-border); }
.sb.gap { background: var(--red-bg); color: #999; border: 1px dashed var(--red-border); }
.sb.future { background: var(--purple-bg); color: var(--purple); border: 1px solid var(--purple-border); }

.seg .si {
  display: flex;
  flex-wrap: wrap;
  gap: 3px;
  margin-top: 3px;
}

.seg .si span {
  font-size: 0.58rem;
  padding: 1px 5px;
  border-radius: 3px;
}

.seg .si .co {
  background: var(--teal-light);
  color: var(--teal);
  border: 1px solid var(--teal-mid);
  font-weight: 600;
}

.seg .si .nt {
  background: var(--bg-off);
  color: var(--text-light);
  border: 1px solid var(--border-light);
  font-style: italic;
}

/* Flow arrow between lane groups */
.fa {
  display: flex;
  align-items: center;
  justify-content: center;
  height: 16px;
  margin-left: 130px;
  position: relative;
}

.fa::before {
  content: '';
  position: absolute;
  left: 30%; right: 30%; top: 50%;
  height: 1px;
  background: var(--border);
}

.fa .fal {
  background: white;
  padding: 0 10px;
  font-size: 0.6rem;
  color: var(--teal);
  font-weight: 600;
  z-index: 1;
}

/* ===== DETAIL VIEW ===== */
#dv { display: none; }

.dl {
  margin-bottom: 24px;
  animation: su 0.4s ease both;
}

@keyframes su { from { opacity: 0; transform: translateY(14px); } to { opacity: 1; transform: translateY(0); } }

.dlh {
  display: flex;
  align-items: center;
  gap: 9px;
  margin-bottom: 10px;
  padding-bottom: 7px;
  border-bottom: 2px solid var(--border-light);
}

.dlh .num {
  width: 26px; height: 26px;
  border-radius: 5px;
  display: flex; align-items: center; justify-content: center;
  font-size: 0.68rem;
  font-weight: 800;
  color: white;
}

.dlh .dtl {
  font-size: 1rem;
  font-weight: 700;
  color: var(--dark);
}

.dg {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
  gap: 9px;
}

.dc {
  background: var(--bg);
  border: 1.5px solid var(--border);
  border-radius: 7px;
  padding: 12px 14px;
  transition: all 0.2s;
}

.dc:hover { border-color: var(--teal-mid); box-shadow: var(--shadow-md); }
.dc.hj { border-left: 3px solid var(--teal); }

.dc .dct { font-size: 0.76rem; font-weight: 700; color: var(--dark); margin-bottom: 5px; }

.dc .ds {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 3px 0;
  border-bottom: 1px solid var(--border-light);
}

.dc .ds:last-child { border-bottom: none; }
.dc .dsn { font-size: 0.68rem; color: var(--text); }
.dc .dsm { display: flex; align-items: center; gap: 4px; }
.dc .dsc { font-size: 0.58rem; color: var(--teal); font-weight: 600; }

/* ===== CONNECTIVITY VIEW ===== */
#cv { display: none; }

.ct { text-align: center; margin-bottom: 16px; }
.ct h2 { font-size: 1.1rem; font-weight: 800; color: var(--dark); }
.ct p { font-size: 0.75rem; color: var(--text-light); margin-top: 2px; }

#csvg {
  width: 100%;
  height: 660px;
  border: 1px solid var(--border-light);
  border-radius: 8px;
  background: var(--bg-off);
}

/* Tooltip */
.tip {
  position: fixed;
  background: var(--dark);
  color: white;
  border-radius: 7px;
  padding: 10px 14px;
  font-size: 0.7rem;
  pointer-events: none;
  z-index: 9999;
  max-width: 260px;
  box-shadow: 0 6px 20px rgba(0,0,0,0.2);
  opacity: 0;
  transition: opacity 0.15s;
  line-height: 1.4;
}
.tip.show { opacity: 1; }
.tip .tt { font-weight: 700; margin-bottom: 2px; }
.tip .tc { color: rgba(255,255,255,0.5); font-size: 0.6rem; margin-bottom: 4px; }
.tip .tb { font-size: 0.58rem; padding: 1px 5px; border-radius: 3px; display: inline-block; }
.tip .tb.core { background: rgba(0,166,118,0.2); color: #69f0ae; }
.tip .tb.partial { background: rgba(212,146,11,0.2); color: #ffd740; }
.tip .tb.gap { background: rgba(255,255,255,0.08); color: rgba(255,255,255,0.4); }
.tip .tb.future { background: rgba(107,79,187,0.2); color: #b388ff; }
.tip .tco { color: rgba(255,255,255,0.6); font-size: 0.6rem; margin-top: 3px; }

.page-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 14px 48px;
  max-width: 1600px;
  margin: 0 auto;
  border-top: 1px solid var(--border);
  font-size: 0.62rem;
  color: var(--text-light);
}

@media (max-width: 768px) {
  .page-header { padding: 20px 16px 0; }
  .page-header h1 { font-size: 1.1rem; }
  .legend-strip { padding: 10px 16px; gap: 10px; }
  .canvas-wrap { padding: 4px 8px 24px; }
  .lane-label { width: 70px; min-width: 70px; font-size: 0.55rem; }
  .dg { grid-template-columns: 1fr; }
}
</style>
</head>
<body>

<div class="page-header">
  <div class="tag-row">
    <span class="tag">Travel Tech Ecosystem</span>
    <span class="tag teal">Juniper Positioning</span>
  </div>
  <h1>The travel technology value chain spans 12 layers from supply creation to compliance, with <em>Juniper positioned as a core connectivity and packaging player</em></h1>
  <p class="subtitle">Travel Technology Distribution Ecosystem — Full taxonomy mapped to the leisure & business travel value chain</p>
</div>

<div class="legend-strip">
  <span class="legend-title">Juniper Presence</span>
  <div class="legend-item"><div class="legend-dot core"></div> Core / Current</div>
  <div class="legend-item"><div class="legend-dot partial"></div> Partial / Emerging</div>
  <div class="legend-item"><div class="legend-dot future"></div> Future</div>
  <div class="legend-item"><div class="legend-dot gap"></div> Gap</div>
  <div class="legend-sep"></div>
  <div class="legend-arrow"><div class="al solid"></div> Juniper highlighted</div>
  <div class="legend-arrow"><div class="al dashed"></div> No presence</div>
</div>

<div class="controls">
  <button class="ctrl-btn active" data-view="vc">Value Chain</button>
  <button class="ctrl-btn" data-view="dv">Detail Grid</button>
  <button class="ctrl-btn" data-view="cv">Connectivity</button>
</div>

<div class="canvas-wrap">
  <div id="vc"></div>
  <div id="dv"></div>
  <div id="cv">
    <div class="ct">
      <h2>Juniper Group — Connectivity Across the Value Chain</h2>
      <p>How Juniper's subsidiaries interconnect across systems and layers</p>
    </div>
    <svg id="csvg"></svg>
  </div>
</div>

<div class="page-footer">
  <span>Source: Company taxonomy, Shiji Distribution Chart 2025/2026, desk research</span>
  <span>Juniper Travel Technology Ecosystem</span>
</div>

<div class="tip" id="tip"></div>

<script>
const D=[{id:1,name:"Core Reservation & Inventory Systems",short:"Supply",lc:"s1",color:"#009B9B",groups:[{label:"Accommodation",items:[{n:"PMS",f:"Property Management System",j:"gap"},{n:"CRS",f:"Central Reservation System",j:"partial",c:"Hotetec"},{n:"RMS",f:"Revenue Management System",j:"gap"},{n:"Housekeeping Mgmt"},{n:"Guest Experience Platform"},{n:"Hotel Operations"},{n:"Hotel Loyalty"}]},{label:"Airlines",items:[{n:"PSS",f:"Passenger Service System",j:"gap"},{n:"Airline Reservations"},{n:"DCS"},{n:"Crew Management"},{n:"Flight Operations"},{n:"Airline Revenue Mgmt"}]},{label:"Rail / Bus / Ground",items:[{n:"Rail Reservation"},{n:"Rail Operations"},{n:"Passenger Info"},{n:"Ticketing & Fares"}]},{label:"Cruise",items:[{n:"Cruise Reservation",j:"current",c:"IST"},{n:"Fleet Operations"},{n:"Shore Excursions"}]}]},{id:2,name:"Distribution & Connectivity Infrastructure",short:"Aggregation & Distribution",lc:"s2",color:"#0086A8",groups:[{label:"Distribution Core",items:[{n:"GDS",f:"Global Distribution System",nt:"Amadeus, Sabre, Travelport"},{n:"Connectivity Hub",f:"Connectivity Hub / Switch",j:"core",c:"Juniper Travel Tech, DS"},{n:"XML / API Aggregation"},{n:"Content Mapping"},{n:"Channel Manager",j:"gap",c:"Hotetec"},{n:"Supplier Extranet"},{n:"Distribution Mgmt"}]},{label:"Air Aggregation",items:[{n:"Air Aggregator",j:"current",c:"Lleego"}]},{label:"Bedbank / Wholesale",items:[{n:"Bedbank Platform",j:"partial",c:"DOME"}]}]},{id:3,name:"Retailing, Selling & Merchandising",short:"Packaging & Retail",lc:"s3",color:"#006B8A",groups:[{label:"Sales Platforms",items:[{n:"Booking Engine",j:"current",c:"Juniper TT, Traveltek, Hotetec, Cangoroo, Inspiretec"},{n:"OTA Platform",j:"gap"},{n:"Metasearch"},{n:"Travel Marketplace"},{n:"B2B Booking Portal"},{n:"Corporate Booking Tool"}]},{label:"Packaging & Merchandising",items:[{n:"Dynamic Packaging",j:"core",c:"Peakwork"},{n:"Offer Management"},{n:"Ancillary Mgmt",j:"partial",c:"Juniper Travel Tech"},{n:"NDC Offer & Order Mgmt"}]}]},{id:4,name:"Travel Agency & Tour Operator Software",short:"TA / TO Software",lc:"s4",color:"#00526D",groups:[{label:"Agency & TO Tools",items:[{n:"Tour Operator System"},{n:"Agency Back-Office"},{n:"Itinerary Builder"},{n:"Quotation / Proposals"},{n:"Contracting & Supplier Mgmt"},{n:"DMC Software"}]}]},{id:5,name:"Vacation Rental & Alt. Accommodation",short:"Vacation Rental",lc:"s4",color:"#00526D",groups:[{label:"Vacation Rental Tech",items:[{n:"Vacation Rental PMS"},{n:"STR Channel Manager"},{n:"Owner Portal"},{n:"Smart Lock / Check-in"},{n:"Guest Messaging"},{n:"Cleaning & Turnover"}]}]},{id:6,name:"Transportation & Mobility",short:"Transport & Mobility",lc:"s5",color:"#3A4A5C",groups:[{label:"Mobility",items:[{n:"Car Rental / Transfers"},{n:"MaaS Platform"},{n:"Route Planning"},{n:"Ticketing Aggregator"}]}]},{id:7,name:"Payments, Fintech & Risk",short:"Payments & Fintech",lc:"s5",color:"#3A4A5C",groups:[{label:"Payments Stack",items:[{n:"Payment Gateway",j:"gap"},{n:"PSP",f:"Payment Service Provider",j:"gap"},{n:"VCC",f:"Virtual Credit Card",j:"partial"},{n:"Fraud Detection",j:"gap"},{n:"Settlement & Reconciliation"},{n:"Travel Insurance"},{n:"BNPL / Financing"}]}]},{id:8,name:"Customer Lifecycle & Engagement",short:"Customer Lifecycle",lc:"s6",color:"#2D3044",groups:[{label:"CRM & Engagement",items:[{n:"Travel CRM",j:"current",c:"Inspiretec"},{n:"Customer Support",j:"partial",c:"Inspiretec"},{n:"Marketing Automation"},{n:"Loyalty & Rewards",j:"gap",c:"Inspiretec"},{n:"CDP",f:"Customer Data Platform",j:"emerging"},{n:"Personalization Engine",j:"future"},{n:"Chatbot / Conversational AI"}]}]},{id:9,name:"Operations, Fulfillment & Experience",short:"Fulfillment & Ops",lc:"s5",color:"#3A4A5C",groups:[{label:"Operations",items:[{n:"Check-in / Kiosk"},{n:"Boarding & Gate Mgmt"},{n:"Airport Operations"},{n:"Trip Management"},{n:"Disruption Mgmt"},{n:"Travel Risk Mgmt"},{n:"Experience Mgmt"}]}]},{id:10,name:"Data, Intelligence & Platform Layer",short:"Data & Intelligence",lc:"s7",color:"#1A1A2E",groups:[{label:"Data & AI",items:[{n:"Data Warehouse",j:"core",c:"Juniper Travel Tech"},{n:"Business Intelligence",j:"current",c:"Juniper Travel Tech"},{n:"Pricing Intelligence"},{n:"Demand Forecasting"},{n:"Recommendation Engine"},{n:"AI Optimization Layer",j:"future"},{n:"Reporting & KPI"}]}]},{id:11,name:"Content, Digital & Commerce Infrastructure",short:"Digital Infra",lc:"s7",color:"#1A1A2E",groups:[{label:"Digital & Commerce",items:[{n:"CMS"},{n:"Digital Experience Platform"},{n:"Search & Recommendation"},{n:"API Management"},{n:"Identity & Access Mgmt"},{n:"Mobile App Platform"}]}]},{id:12,name:"Compliance, Security & Regulatory",short:"Compliance",lc:"s7",color:"#1A1A2E",groups:[{label:"Compliance & Regulatory",items:[{n:"KYC / Identity Verification"},{n:"Border & Immigration"},{n:"Duty of Care"},{n:"Regulatory Reporting"}]}]}];

const JC=[
  {name:"Juniper Travel Tech",role:"Connectivity Hub / Booking / BI / Data",sys:["Connectivity Hub","Booking Engine","Business Intelligence","Data Warehouse","Ancillary Mgmt"],color:"#009B9B"},
  {name:"Peakwork",role:"Dynamic Packaging Engine",sys:["Dynamic Packaging"],color:"#006B8A"},
  {name:"Hotetec",role:"Channel Mgmt / Booking",sys:["CRS","Channel Manager","Booking Engine"],color:"#0086A8"},
  {name:"Inspiretec",role:"CRM / Support / Booking",sys:["Travel CRM","Customer Support","Loyalty & Rewards","Booking Engine"],color:"#2D8A5C"},
  {name:"IST",role:"Cruise Reservation",sys:["Cruise Reservation"],color:"#3A6B8A"},
  {name:"Lleego",role:"Air Aggregation",sys:["Air Aggregator"],color:"#D4920B"},
  {name:"Traveltek",role:"Booking Technology",sys:["Booking Engine"],color:"#6B4FBB"},
  {name:"Cangoroo",role:"Booking Platform",sys:["Booking Engine"],color:"#A0527A"},
  {name:"DOME",role:"Bedbank / Wholesale",sys:["Bedbank Platform"],color:"#5C3A6B"},
  {name:"DS",role:"Distribution Services",sys:["Connectivity Hub"],color:"#009B9B"},
];

const tip=document.getElementById('tip');
function showT(e,h){tip.innerHTML=h;tip.classList.add('show');const r=tip.getBoundingClientRect();let x=e.clientX+12,y=e.clientY+12;if(x+r.width>window.innerWidth-12)x=e.clientX-r.width-12;if(y+r.height>window.innerHeight-12)y=e.clientY-r.height-12;tip.style.left=x+'px';tip.style.top=y+'px';}
function hideT(){tip.classList.remove('show');}

function badge(j){const m={core:['Core','core'],current:['Current','core'],partial:['Partial','partial'],gap:['Gap','gap'],emerging:['Emerging','future'],future:['Future','future']};const v=m[j];return v?`<span class="sb ${v[1]}">${v[0]}</span>`:''}
function tipB(j){const m={core:['🟢 Core Strength','core'],current:['🟢 Current','core'],partial:['🟡 Partial','partial'],gap:['⚪ Gap','gap'],emerging:['🟣 Emerging','future'],future:['🟣 Future','future']};const v=m[j];return v?`<span class="tb ${v[1]}">${v[0]}</span>`:''}

// VALUE CHAIN
function buildVC(){
  const w=document.getElementById('vc');w.innerHTML='';
  let prevLC='';
  D.forEach((l,i)=>{
    if(i>0&&l.lc!==prevLC){const a=document.createElement('div');a.className='fa';a.innerHTML='<span class="fal">▼</span>';w.appendChild(a);}
    prevLC=l.lc;
    const lane=document.createElement('div');lane.className='swim-lane';
    const lb=document.createElement('div');lb.className=`lane-label ${l.lc}`;lb.innerHTML=`<span>${l.id}. ${l.short}</span>`;lane.appendChild(lb);
    const bd=document.createElement('div');bd.className='lane-body';
    l.groups.forEach(g=>{
      const ge=document.createElement('div');ge.className='grp';ge.innerHTML=`<span class="gl">${g.label}</span>`;
      g.items.forEach(it=>{
        const isH=it.j&&(it.j==='core'||it.j==='current');
        const s=document.createElement('div');s.className=`seg${isH?' jh':''}`;
        let h='<div class="sh">';
        h+=`<span class="sn">${it.n}</span>`;
        if(it.j)h+=badge(it.j);
        h+='</div>';
        if(it.c){h+='<div class="si">';it.c.split(',').forEach(c=>{h+=`<span class="co">${c.trim()}</span>`;});h+='</div>';}
        if(it.nt)h+=`<div class="si"><span class="nt">${it.nt}</span></div>`;
        s.innerHTML=h;
        s.addEventListener('mouseenter',e=>{let t=`<div class="tt">${it.f||it.n}</div><div class="tc">${l.name} › ${g.label}</div>`;if(it.j)t+=tipB(it.j);if(it.c)t+=`<div class="tco">${it.c}</div>`;showT(e,t);});
        s.addEventListener('mousemove',e=>{const r=tip.getBoundingClientRect();let x=e.clientX+12,y=e.clientY+12;if(x+r.width>window.innerWidth-12)x=e.clientX-r.width-12;if(y+r.height>window.innerHeight-12)y=e.clientY-r.height-12;tip.style.left=x+'px';tip.style.top=y+'px';});
        s.addEventListener('mouseleave',hideT);
        ge.appendChild(s);
      });
      bd.appendChild(ge);
    });
    lane.appendChild(bd);w.appendChild(lane);
  });
  const tv=document.createElement('div');tv.className='fa';tv.innerHTML='<span class="fal" style="font-size:0.7rem">▼ Traveler</span>';w.appendChild(tv);
}

// DETAIL
let db=false;
function buildDV(){
  if(db)return;db=true;
  const w=document.getElementById('dv');
  D.forEach((l,i)=>{
    const s=document.createElement('div');s.className='dl';s.style.animationDelay=(i*0.04)+'s';
    const hd=document.createElement('div');hd.className='dlh';hd.innerHTML=`<div class="num" style="background:${l.color}">${l.id}</div><div class="dtl">${l.name}</div>`;s.appendChild(hd);
    const g=document.createElement('div');g.className='dg';
    l.groups.forEach(gr=>{
      const c=document.createElement('div');const hj=gr.items.some(i=>i.j&&i.j!=='gap');c.className=`dc${hj?' hj':''}`;
      let h=`<div class="dct">${gr.label}</div>`;
      gr.items.forEach(it=>{
        h+=`<div class="ds"><span class="dsn">${it.f||it.n}</span><div class="dsm">`;
        if(it.c)h+=`<span class="dsc">${it.c.split(',')[0].trim()}</span>`;
        if(it.j)h+=badge(it.j);
        h+='</div></div>';
      });
      c.innerHTML=h;g.appendChild(c);
    });
    s.appendChild(g);w.appendChild(s);
  });
}

// CONNECTIVITY
function drawCV(){
  const svg=document.getElementById('csvg');svg.innerHTML='';
  const r=svg.getBoundingClientRect();const W=r.width||1100,H=660;
  svg.setAttribute('viewBox',`0 0 ${W} ${H}`);
  const cx=W/2,cy=H/2,cR=155,sR=285;

  const defs=document.createElementNS('http://www.w3.org/2000/svg','defs');
  defs.innerHTML=`<filter id="sh"><feDropShadow dx="0" dy="1" stdDeviation="2" flood-opacity="0.08"/></filter><marker id="ah" markerWidth="6" markerHeight="4" refX="6" refY="2" orient="auto"><polygon points="0 0,6 2,0 4" fill="#009B9B" opacity="0.5"/></marker>`;
  svg.appendChild(defs);

  [cR,sR].forEach(rad=>{const c=document.createElementNS('http://www.w3.org/2000/svg','circle');c.setAttribute('cx',cx);c.setAttribute('cy',cy);c.setAttribute('r',rad);c.setAttribute('fill','none');c.setAttribute('stroke','#E2E4EA');c.setAttribute('stroke-width','1');c.setAttribute('stroke-dasharray','5 3');svg.appendChild(c);});

  const hg=document.createElementNS('http://www.w3.org/2000/svg','g');
  hg.innerHTML=`<circle cx="${cx}" cy="${cy}" r="42" fill="#E6F5F5" stroke="#009B9B" stroke-width="2" filter="url(#sh)"/><text x="${cx}" y="${cy-7}" text-anchor="middle" fill="#009B9B" font-family="Plus Jakarta Sans" font-size="11" font-weight="800">JUNIPER</text><text x="${cx}" y="${cy+7}" text-anchor="middle" fill="#006B8A" font-family="Plus Jakarta Sans" font-size="8" font-weight="500">GROUP</text>`;
  svg.appendChild(hg);

  const allS=new Set();JC.forEach(c=>c.sys.forEach(s=>allS.add(s)));const sys=Array.from(allS);
  const sP={};
  sys.forEach((s,i)=>{const a=(i/sys.length)*Math.PI*2-Math.PI/2;const x=cx+Math.cos(a)*sR,y=cy+Math.sin(a)*sR;sP[s]={x,y};const tw=s.length*5.2+14;const g=document.createElementNS('http://www.w3.org/2000/svg','g');g.innerHTML=`<rect x="${x-tw/2}" y="${y-10}" width="${tw}" height="20" rx="4" fill="white" stroke="#E2E4EA" stroke-width="1" filter="url(#sh)"/><text x="${x}" y="${y+3}" text-anchor="middle" fill="#1A1A2E" font-family="Plus Jakarta Sans" font-size="7.5" font-weight="600">${s}</text>`;svg.appendChild(g);});

  const cP={};
  JC.forEach((co,i)=>{const a=(i/JC.length)*Math.PI*2-Math.PI/2;const x=cx+Math.cos(a)*cR,y=cy+Math.sin(a)*cR;cP[co.name]={x,y};
    const ln=document.createElementNS('http://www.w3.org/2000/svg','line');ln.setAttribute('x1',cx);ln.setAttribute('y1',cy);ln.setAttribute('x2',x);ln.setAttribute('y2',y);ln.setAttribute('stroke',co.color+'20');ln.setAttribute('stroke-width','1.5');svg.appendChild(ln);
    const nm=co.name.length>13?co.name.substring(0,11)+'…':co.name;
    const rl=co.role.length>20?co.role.substring(0,18)+'…':co.role;
    const g=document.createElementNS('http://www.w3.org/2000/svg','g');g.innerHTML=`<circle cx="${x}" cy="${y}" r="25" fill="${co.color}10" stroke="${co.color}" stroke-width="1.5" filter="url(#sh)"/><text x="${x}" y="${y-4}" text-anchor="middle" fill="${co.color}" font-family="Plus Jakarta Sans" font-size="7" font-weight="700">${nm}</text><text x="${x}" y="${y+6}" text-anchor="middle" fill="#8888A0" font-family="Plus Jakarta Sans" font-size="5.5">${rl}</text>`;svg.appendChild(g);
  });

  JC.forEach(co=>{const f=cP[co.name];if(!f)return;co.sys.forEach(s=>{const t=sP[s];if(!t)return;
    const p=document.createElementNS('http://www.w3.org/2000/svg','path');const cpx=(f.x+cx)*0.5,cpy=(f.y+cy)*0.5;
    p.setAttribute('d',`M${f.x},${f.y} Q${cpx},${cpy} ${t.x},${t.y}`);p.setAttribute('fill','none');p.setAttribute('stroke',co.color);p.setAttribute('stroke-width','1.2');p.setAttribute('stroke-opacity','0.22');p.setAttribute('marker-end','url(#ah)');
    p.addEventListener('mouseenter',e=>{p.setAttribute('stroke-width','2.5');p.setAttribute('stroke-opacity','0.65');showT(e,`<div class="tt">${co.name} → ${s}</div><div class="tc">${co.role}</div>`);});
    p.addEventListener('mouseleave',()=>{p.setAttribute('stroke-width','1.2');p.setAttribute('stroke-opacity','0.22');hideT();});
    svg.appendChild(p);
  });});
}

// VIEW TOGGLE
const vEls={vc:document.getElementById('vc'),dv:document.getElementById('dv'),cv:document.getElementById('cv')};
document.querySelectorAll('.ctrl-btn').forEach(b=>{b.addEventListener('click',()=>{
  document.querySelectorAll('.ctrl-btn').forEach(x=>x.classList.remove('active'));b.classList.add('active');
  const v=b.dataset.view;Object.entries(vEls).forEach(([k,el])=>el.style.display=k===v?'block':'none');
  if(v==='dv')buildDV();if(v==='cv')drawCV();
});});

buildVC();
window.addEventListener('resize',()=>{if(vEls.cv.style.display==='block')drawCV();});
</script>
</body>
</html>
